# ──────────────────────────────────────────────────────────────
# SkillEX — Frontend Environment Variables (Vite)
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to git.
#
# ⚠️  All API keys (Google OAuth, Gemini AI, JWT secret, DB creds)
#     belong ONLY in the backend. See:
#     backend/src/main/resources/application-local.properties.example
# ──────────────────────────────────────────────────────────────

# ── Frontend (Vite) ────────────────────────────────────────────
# Public URL of the deployed frontend
VITE_APP_URL=http://localhost:5173

# Spring Boot API base URL.
# Leave empty in dev — Vite proxies /api → http://localhost:8080 automatically.
# Set to your production API URL for production builds.
VITE_API_URL=